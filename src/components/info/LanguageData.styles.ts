import styled from "styled-components";
import tw from "twin.macro";

export const LanguageDataDetails = styled.div`
    p {
        ${tw`mt-3`}
    }

    p:first-child {
        ${tw`mt-0`}
    }
`;
